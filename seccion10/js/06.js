//Objeto Math
let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); //Redondear hacia arriba
resultado = Math.floor(2.9); //Redondear hacia abajo
resultado = Math.sqrt(144); //Raíz cuadrada
resultado = Math.abs(-144); //Convertir a positivo 
resultado = Math.min(10, 11, 1, 200, 134); //Obtiene el número más pequeño del listado
resultado = Math.max(10, 11, 1, 200, 134); //Obtiene el número más grande del listado
resultado = Math.random(); //Obtener número aletorio
resultado = Math.floor(Math.random()*30); //Obtener número aletorio

console.log(resultado);